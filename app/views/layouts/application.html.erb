<!DOCTYPE html>
<html>
  <head>
    <title>Prevozi</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>

  <body>

<div class="mdl-layout mdl-js-layout mdl-layout--fixed-header mdl-layout--no-drawer-button">
  <header class="mdl-layout__header">
    <div class="mdl-layout__header-row">
      <!-- Title -->
      <span class="mdl-layout-title"><%= link_to 'Prevoz', root_path, {id: 'header-title'} %></span>
      <!-- Add spacer, to align navigation to the right -->
      <div class="mdl-layout-spacer"></div>
      <!-- Navigation. We hide it in small screens. -->
      <nav class="mdl-navigation">
        <% if logged_in? %>
          <%= link_to 'Logout', logout_path, {class: 'mdl-navigation__link'} %>
        <% else %>
          <%= link_to 'Login', login_path, {class: 'mdl-navigation__link'} %>
          <%= link_to 'Register', register_path, {class: 'mdl-navigation__link'} %>
        <% end %>
      </nav>
    </div>
  </header>
    <%= yield %>
    </div>
    <div class="popup-message">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, id: "flash_#{name}", class: ['mdl-shadow--2dp', 'flash'] %>
      <% end %>
    </div>
  </body>
</html>
